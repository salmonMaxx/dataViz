name: torsdags_test
description: Denna gång är det torsdag och testerna pågår!


version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  #the two below are only temporary
  firebase_auth:
  google_sign_in:
  launcher_assist: ^1.1.3
  cupertino_icons: ^0.1.2
  location: ^2.3.5
  #geolocation: ^4.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

assets:
  # These are local on a computer, need to add to project
  - Pictures/nobel1.jpg
  - Pitcures/nobel2.jpg
  - assets/logo_dataviz_1.png