name: torsdags_test
description: Denna gång är det torsdag och testerna pågår!


version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  #the two below are only temporary
  firebase_auth:
  google_sign_in:
  launcher_assist: ^1.1.3
  cupertino_icons: ^0.1.2
  permission_handler: '^3.0.0'

  location: ^2.3.5
  #geolocation: ^4.0.1

  call_log: ^2.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  assets:
  - assets/logo_dataviz_1.png
  - assets/dataViz_logo2.png
  - assets/mabu-lightbulb.png
  - assets/Light_pic.png
  - assets/light.png
  - assets/blomma_test.jpg
  - assets/test1.jpg

  fonts:
  - family: Modak-Regular
    fonts:
    - asset: assets/Modak-Regular.ttf

  - family: Abel-Regular
    fonts:
    - asset: assets/Abel-Regular.ttf
      weight: 900


  uses-material-design: true

